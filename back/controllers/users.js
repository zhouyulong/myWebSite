const User = require('../models/userModel');

// 获取用户列表
let getUsers = async (ctx, next) => {
    try{

    }catch (e) {
        ctx.body = {}
    }
}

// 新增用户


// 根据 username 删除用户


// 修改用户 nickname 或 phone